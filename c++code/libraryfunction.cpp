#include <iostream>
#include <cmath>   // math library
using namespace std;

int main() {
    cout << "Square root of 16 = " << sqrt(16) << endl;
    cout << "Power (2^5) = " << pow(2, 5) << endl;
    return 0;
}
